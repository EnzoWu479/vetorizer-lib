# vetorizer-lib Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-17

## Active Technologies
- Python 3.10+ (backend), TypeScript (frontend) + FastAPI (backend API), React + TailwindCSS (frontend), vetorizer_lib (embedding/storage) (002-web-gui)
- Qdrant vector database (file-based persistence), SQLite for metadata (002-web-gui)

- Python 3.10+ (uv-managed) + sentence-transformers, qdrant-client, transformers, torch, Pillow, pandas (1-vector-embedding-lib)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.10+ (uv-managed): Follow standard conventions

## Recent Changes
- 002-web-gui: Added Python 3.10+ (backend), TypeScript (frontend) + FastAPI (backend API), React + TailwindCSS (frontend), vetorizer_lib (embedding/storage)

- 1-vector-embedding-lib: Added Python 3.10+ (uv-managed) + sentence-transformers, qdrant-client, transformers, torch, Pillow, pandas

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
